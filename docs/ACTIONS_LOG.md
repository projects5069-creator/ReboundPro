# ReboundPro — Actions Log

יומן-פעולות **append-only**: כל סשן עבודה מוסיף ערך מתוארך (תאריך · commit hash · תקציר) בסוף, לפני `## Next`. לא עורכים/מוחקים ערכים קיימים — רק מוסיפים. זהו התיעוד הכרונולוגי של מה נעשה ומתי (נגזר מ-git history).

| תאריך | commit | פעולה |
|-------|--------|-------|
| 2026-06-13 | `1196418` | M1: live collector (scanner + post-analysis) + daily workflow |
| 2026-06-14 | `6f58292` | M4-collect: snapshot פונדמנטלי point-in-time (Finviz) + monitoring dashboard |
| 2026-06-14 | `e64f05f` | M2: intraday scanner + חלון post-analysis 20 יום |
| 2026-06-14 | `8949557` | docs: חוקי-עבודה ל-CLAUDE.md (מיזוג, ללא מחיקה) |
| 2026-06-14 | `e82430a` | intraday_scanner: הגנת-שעות עצמית (לוח NYSE + 09:30–16:00 ET) |
| 2026-06-14 | `0fa8e72` | M4-collect: לכידת חדשות point-in-time (catalyst.py, Finnhub) |
| 2026-06-14 | `22c218c` | daily_summary (בריאות-איסוף) + תמיכת Streamlit Cloud creds + רענון state |
| 2026-06-14 | `5acf733` | deps: הוספת plotly ל-requirements.txt |
| 2026-06-14 | `acc4c23` | gitignore: streamlit_cloud_secrets.toml |
| 2026-06-14 | `882a9b2` | chore: force rebuild ל-Streamlit Cloud |
| 2026-06-14 | `40ad829` | deps: הצהרת תלויות dashboard ב-pyproject + uv.lock (Cloud צורך uv.lock) |
| 2026-06-14 | _(this commit)_ | docs: מערך-תיעוד מלא (MASTERPLAN/ACTIONS_LOG/TASKS/PROJECT_KNOWLEDGE) + חיווט-זיכרון ב-CLAUDE.md |
| 2026-06-14 | _(this commit)_ | M3: מעקב מדורג (`intraday_timeseries.py` + טאב + צעד workflow, D0–D3 10ד'/D4–D20 ~3/יום) · כרטיס-מניה בדashboard (intraday/post/fundamentals-Finviz/news, view-only) · תיקוני-תצוגה רוחביים (`%` בתא/פסיקים/2-ספרות + חשיפת שדות-מסלול ב-watchlist) |
| 2026-06-14 | _(this commit)_ | M3.4: מקור-איסוף `gradual_drop` (`gradual_scanner.py` + צעד ב-daily.yml בין scanner ל-catalyst) — close היום ≥10% מתחת ל-5 ימי-מסחר אחורה (Finviz `Performance: Week -10%`, אומת חי 422 מועמדים + ADBE -18.86%) · עמודה `drop_kind` migration-safe + תיוג intraday_drop ב-scanner/intraday_scanner · דדופ חוצה-סוגים 20 ימי-מסחר · dashboard סינון/תצוגה לפי drop_kind · אזהרת value-trap מתועדת |
| 2026-06-14 | _(this commit)_ | M3.5: dashboard → Streamlit multipage (דף לכל drop_kind). חילוץ כל הלוגיקה ל-`dashboard_common.py` (אפס כפילות); `dashboard.py`=דף-בית; `pages/1_Intraday_Drop.py`+`pages/2_Gradual_Drop.py` כל אחד `render(drop_kind)` עם 5 הטאבים מסונן מראש (בלי multiselect drop_kind). אומת headless ב-AppTest: בית+2 דפים, 0 חריגות, 5 טאבים/דף; audit config.* נקי |
| 2026-06-14 | _(this commit)_ | tag-backfill: `scanner.py --backfill-tags` — מילוי drop_kind/source ל-23 שורות-legacy מ-6/12 (drop_kind=intraday_drop; source מוסק: intraday אם יש שדות-מסלול, אחרת eod_close — כאן כולן eod_close). upsert חלקי merge-safe (לא דורס שדות אחרים). **הרצה חיה:** 23 עודכנו, 0 blank drop_kind, ASTS שמר price/context. **health_monitor --morning → exit=0 בריא** (field-completeness ⚠️→✅; הייתה האזהרה האחרונה). תיקון-נתון, לא השתקת-בדיקה. |
| 2026-06-14 | _(this commit)_ | M3-monitor-4: (1) "שוק-סגור≠אזהרה" — intraday-freshness תלוי-שוק: היום אינו יום-מסחר → **🌙 מצב-רגוע** (severity=ok, אייקון ייעודי, לא משפיע על exit); יום-מסחר ו-0 intraday → ⚠️. (2) פירוט-בדיקות נגיש: `details_text` ב-health_log (כל השורות) + **expander לכל ריצה** ב-pages/3. תיקון אגב: UnboundLocalError על `pd` (import מקומי כפול הוסר; pd ל-top-level). אומת חי: morning/evening — intraday=🌙 (לא ⚠️), exit ירד ⚠️2→⚠️1 (נשאר 1 כי field-completeness legacy), details_text נכתב (readback), AppTest 4 דפים 0-חריגות (pages/3 expander). רק health_log נכתב. |
| 2026-06-14 | _(this commit)_ | health.yml: כוונון זמני-cron — `0 13`+`30 17 * * 1-5` (--morning) ו-`0 1 * * 2-6` (--evening, 01:00 UTC=21:00 ET היום-הקודם, ~5ש' אחרי daily.yml במקום שעה → לא מדווח "חסר" בטעות על EOD מתעכב). **DOW ערב = 2-6** (שלישי–שבת UTC) לכיסוי מסחר שני–שישי. flag-mapping עודכן. MONITORING.md: טבלה+הערת-DST (פרו 08:00/12:30/20:00). שינוי-workflow בלבד. אומת: YAML parse, 3 crons, DOW. |
| 2026-06-14 | _(this commit)_ | M3-monitor-3: (א) תיקון קריסת-חי של pages/3 — `render_system_health`/`render_health_banner` פותרים sheet_id פנימית (אותו מנגנון כמו `render()` ב-pages/1/2); הדפים קוראים בלי arg. **שורש אמיתי:** הקוד תקין (resolve_sheet_id קיים+עובד בבית) — הקריסה החיה = cache-מודול תקוע ב-Cloud (pages/3 חדש מול dashboard_common ישן חסר render_system_health); תוקן ע"י redeploy-marker. ה-AppTest תפס באג שני: `int('')` ב-_stock_card (lookback ריק → ValueError) — תוקן (guard). אומת: AppTest כל 4 הדפים 0-חריגות. (ב) `.github/workflows/health.yml` — 3 ריצות/יום-חול (13/17 UTC `--morning`, 23:30 UTC `--evening`), mode לפי schedule, exit=2-בלבד-מכשיל, GITHUB_STEP_SUMMARY, workflow_dispatch. secrets/deps כמו daily.yml. YAML תקין. |
| 2026-06-14 | _(this commit)_ | M3-monitor-2: דף System Health + סטטוס-על. health_monitor כותב טאב-בקרה `health_log` ב-Sheet (`HEALTH_LOG_HEADER`; היחיד שנכתב — שאר הטאבים read-only; דילוג-שקט בלי creds) + jsonl מקומי. `pages/3_System_Health.py` (היסטוריה+גרף-מגמה+טבלה מסוננת) + באנר-סטטוס בעמוד-הבית (ריצה אחרונה + דגל >24ש'). אומת חי: morning+evening כתבו 2 שורות ל-health_log (readback, header תואם), grep מאשר רק health_log נכתב, AppTest בית+page3 0-חריגות. redeploy-marker bumped |
| 2026-06-14 | _(this commit)_ | M3-monitor: `health_monitor.py` — סוכן-בקרת-בריאות (10 בדיקות / 5 עמודים: Freshness/Volume/Schema/Field/Ops), READ-ONLY מול Sheet (רק read_rows), `--morning`/`--evening`, exit 0/1/2, `health_log.jsonl` מקומי (.gitignore). **בקרת-צינור בלבד, לא edge.** + `docs/MONITORING.md` (runbook). הרצה חיה אומתה: morning+evening exit=2 (schema-drift אמיתי: post_analysis header חסר 14 עמודות חדשות — ייפתר בריצת post הבאה). READ-ONLY מאומת (אפס upsert/update). עדכון הפניה ב-MASTERPLAN §8 |
| 2026-06-14 | _(this commit)_ | docs: הוספת §8 "מפת-דרכים M0→M6" ל-MASTERPLAN (סיכום אבני-דרך לחזרה עתידית; M0–M2 ✅, M3 נוכחי + יעד-יציאה, M4 גייט/edge, M5 מנוע, M6 paper; "הנדסה ≠ הוכחת-edge, NO-GO=ניצחון מתודולוגי") |
| 2026-06-14 | `8c50749` | M3.8.1-fix+run: תיקון targeting (עמודות נעדרות מה-header החי = חסר) + **הרצה חיה**: 23/23 שורות 6/12 מולאו ב-8 שדות-הקשר; readback אישר (עמודות נוספו, ערכים שפויים, שדות קיימים נשמרו). PWRL בלבד עם d20/d60 ריקים (היסטוריה קצרה — תקין). תצפית: ל-23 הישנות `drop_kind`/`source` ריקים (legacy טרום-M3.4; dashboard עושה coalesce→intraday_drop) — מחוץ לסקופ. |
| 2026-06-14 | _(this commit)_ | M3.8.1: backfill הקשר חד-פעמי (`scanner.py --backfill-context`) — ממלא 8 שדות-הקשר (M3.6+M3.7) לכל שורת watchlist חסרה, **point-in-time** לפי scan_date של השורה (`_context_for_row`/`backfill_missing_context`), upsert חלקי merge-safe (לא דורס מלאות), לא ב-workflow. **תיקון באג:** cache של `vix_close`/`etf_momentum` עכשיו לפי (symbol,scan_date) — נכון רב-תאריכי. אומת: ASTS/RKLB @6/12 שפויים, cache-fix (6/12≠5/1), dry-run לא כותב, רק חסרים. (הרצה חיה על ה-23 ע"י עמיחי — דורש creds) |
| 2026-06-14 | _(this commit)_ | M3.8: split/halt detector ב-post_analysis (הגנת-תקפות M4) — flag לא-הרסני `split_halt_flag`+`split_halt_reason`; yf split-feed ראשי (עמודת Stock Splits, אפס קריאה) + גיבוי jump>`SPLIT_HALT_JUMP_PCT=100` + halt_gap. dashboard: הדגשה+מונה contamination ב-Post/Card/Health. MASTERPLAN §5: החרגה מאגרגטים. אומת: CENN 1:60 reverse-split(2026-04-13)→מסומן, AAPL נקי, 4 מסלולי-detector, HEADER 31 מיושר, AppTest 0-חריגות |
| 2026-06-14 | _(this commit)_ | M3.7: 3 סממני-הקשר מבוססי-מחקר (vix_level→Nagel, drop_day_rel_volume→capitulation, sector_momentum_5d/20d→enhanced-reversal) ב-scanner+gradual — VIX+sector פעם-לריצה (`vix_close`/`etf_momentum` cache), rel_vol מ-history קיים. + תיקון פער M3.6: `backfill_intraday_prior_context` ממלא prior-decline לשורות source=intraday בריצת-EOD. עמודות migration-safe; dashboard מציג קבוצת "סממני-הקשר". אומת: compile(9)+audit+יח'(VIX 17.68/XLK mom/ASTS rel-vol 2.11×/backfill AAPL merge-safe)+gradual regression+AppTest 0-חריגות. **פיצ'רים לא אותות; הכרעה M4** |
| 2026-06-14 | _(this commit)_ | M3.6: שני מדדי-איסוף תיאוריים (פיצ'רים, **לא אותות**). recovery-from-trough ב-post_analysis (trough_price/day/recovery/max_recovery — אומת AAPL 2026-04-08); prior-decline ב-watchlist (scanner+gradual): pct_from_52w_high/low + prior_decline_20d/60d, helper משותף `scanner.prior_context`, חלון EOD הורחב ל-`EOD_HISTORY_DAYS=400` (אותה קריאה, אומת ADBE: -51% מהשיא/+3.62% מהשפל). עמודות migration-safe; dashboard מציג. אומת: compile(9)+audit+regression+AppTest enriched (0 חריגות) |
| 2026-06-15 | _(this commit)_ | dashboard-fix: גלישה אופקית אופסה (תצוגה בלבד, אפס שינוי לוגיקה/נתונים). שורש: ב-Streamlit 1.58 `st.dataframe` הוא canvas-grid שמתעלם מ-CSS, לא שובר-שורות בתא, ומוסיף גלילה אופקית כשיש הרבה עמודות. פתרון יציב: helper משותף `show_table()` ב-`dashboard_common` שמרנדר את אותן טבלאות-`styled()` כ-HTML עם CSS (`table-layout:fixed; width:100%; word-break`) → כל העמודות נשמרות, התא שובר-שורות, אפס גלילה אופקית. כל 7 קריאות `st.dataframe` הוחלפו (watchlist/post/health/summary/fund/news/card). + `SHORT_LABELS` (קיצור-כותרות תצוגה בלבד, שמות-עמודות/נתונים ללא שינוי). `layout="wide"` כבר היה פעיל בכל הדפים. RTL נשמר (טבלאות-דאטה LTR כמו קודם). אומת: py_compile(5) + `tests/test_dashboard_overflow.py` 7/7 (4 דפים 0-חריגות עם דאטה סינתטית, rb-table מרונדר, קיצור-כותרת+שימור-עמודות, הדגשת-contamination שורדת apply→relabel→to_html). redeploy-marker → 2026-06-15a |
| 2026-06-15 | _(this commit)_ | fix-429 (שלב א): מיתון חריגת מכסת-קריאות Google Sheets (429 'Read requests per minute per user') שהפילה intraday (כל 10ד')+health+dashboard. **שורש:** כל `read_rows`=2 קריאות-API; SA יחיד משותף → חריגת 60/דקה. **תיקון:** (1) `sheets_manager.batch_read()` — `spreadsheets_get`+`values:batchGet` קוראים את כל הטאבים ב-**2 קריאות** במקום 2 לכל-טאב (render: ~12→2; מאומת ביח'); (2) `_retry_read` backoff על 429 ב-read path (read_rows+batch_read); (3) dashboard עמידות: `try/except APIError`→`QUOTA_MSG` ידידותי ב-render_health_banner/render_system_health/render/home (לא traceback שמפיל עמוד); (4) cache TTL 300→900ש'; render עובר ל-`load_many` (batch אחד ל-6 טאבים). אומת: py_compile(5) + 13/13 tests (batch=2 קריאות/retry/missing-tab/exhaust + resilience 429→הודעה + 4 דפים render). הערה: אימות-חי נחסם זמנית ע"י עצם חריגת-המכסה (intraday עדיין נכשל כל 10ד' עד שהפריסה מקטינה עומס). redeploy-marker→2026-06-15b |
| 2026-06-15 | _(this commit)_ | fix-health-trigger (שלב ב): אבחון — ה-cron הנייטיב של GitHub **לא יורה כלל** בריפו (event=schedule total_count=0 ל-3 ה-workflows מאז יצירה; best-effort ולא אמין בריפו חדש). intraday חי דרך pinger ב-cron-job.org; ל-health לא היה. **תיקון (ללא שינוי-קוד):** mode-selection כבר נופל ל-`inputs.mode` על dispatch (אומת, health.yml:61); נוסף ל-MONITORING.md 3 ה-jobs המדויקים ל-cron-job.org (URL workflow_dispatch של health.yml + method/headers/PAT + body ref=main/inputs.mode + DOW 13:00&17:30 Mon-Fri morning, 01:00 Tue-Sat evening). cron נייטיב נשאר כגיבוי. |
| 2026-06-15 | _(this commit)_ | **fix-429 שורש (הפרדת-infra):** אובחן (מדידה ישירה מ-creds) שה-429 נבע מכך ש-ReboundPro חלק **SA+project עם RidingHigh Pro** (`ridinghigh-sheets-v2@ridinghigh-pro-v2`, consumer 591299446687) → תחרות על דלי **60-reads/min/user** משותף; הסקאנרים של RH (כל דקה) הם הצרכן הדומיננטי, intraday של ReboundPro ~6 reads/10דק' זניח. batch/retry/resilience (התיקונים הקודמים) לא הספיקו — בעיה ארכיטקטונית. **פתרון:** project GCP נפרד **`reboundpro-v1`** (number 737740780440) + SA חדש **`reboundpro-sheets@reboundpro-v1`** → דלי-מכסה עצמאי. הופעלו Sheets+Drive API; ה-Sheet (…zkvjn_f4) שותף עם ה-SA כ-Editor; ה-key הוחלף ב-3 מקומות (creds מקומי · GitHub secret `GOOGLE_CREDENTIALS_JSON` · Streamlit Cloud secret). אפס שינוי-קוד. **אומת:** קריאה מקומית נקייה (project_id=reboundpro-v1, ללא 429) · ריצת intraday 19:10Z (ראשונה על ה-secret החדש) = success אחרי רצף-כשלים · dashboard ב-Cloud מציג `SA: reboundpro-sheets@reboundpro-v1` ונטען ללא 429. ה-SA הישן הושאר על ה-Sheet כגיבוי; לא נגענו במשאבי RidingHigh. |
| 2026-06-15 | _(this commit)_ | dashboard-fix: פיצול טבלת watchlist (תצוגה בלבד, אפס שינוי לוגיקה/נתונים). הבעיה: show_table פתר scroll אופקי אבל ~31 העמודות המוצגות נדחסו לבלתי-קריאות. פתרון: `_watchlist()` מפצל ל-**3 טבלאות-נושא אנכיות** (🪪 זיהוי+צניחה · 📐 הקשר+מומנטום · ⏱️ מסלול-תוך-יומי+מקור), `ticker` עוגן בראש כל אחת, ה-view ממוין **פעם אחת** כך שהשורות מיושרות בין הטבלאות. חל על Intraday+Gradual (render משותף); עמודות drop_kind-ספציפיות מסתננות לבד (אפס השמטה). post_analysis/health_log הושארו (post borderline אבל שומר על הדגשת-contamination; health = tokens צרים קריאים). אומת: py_compile + **15/15 tests** (4 דפים 0-חריגות; אפס-השמטת-עמודה ל-intraday+gradual — כל עמודה מוצגת נמצאת בקבוצה; 3 הקבוצות מרונדרות). redeploy-marker→2026-06-15c |
| 2026-06-16 | _(this commit)_ | **fix-wipe-bug:** תיקון הבאג שמחק את `watchlist_live` הבוקר (~14:20 UTC). שורש: `upsert_rows`/`upsert_by_key` עשו `ws.clear()` ואז `ws.update()`; float לא-JSON-תקין (inf/NaN מטיקר delisted $LILKV) הקריס את `update()` **אחרי** ש-`clear()` כבר רוקן → אובדן-מלא. תיקון ב-`sheets_manager` (שכבת-I/O יחידה → מכסה את כל הכותבים): (1) `_json_safe`/`_sanitize` — inf/-inf/NaN/None→"" לפני כתיבה; (2) `_write_matrix` — **update-first** ב-A1 בלי `clear()` מקדים + blank-pad לכיסוי השורות-הישנות (כשל-update משאיר נתונים ישנים שלמים, לא מוחק). TDD: 5 טסטים (`tests/test_sheets_atomic_write.py`) — רגרסיית-המחיקה (ב) נכשלה-לפני (clear רוקן→assert EXISTING נכשל) ועוברת-אחרי; +sanitize/inf-no-crash/trailing-blank. אומת: py_compile + **20/20 suite**. **טרם שוחזרו נתונים** (intraday disabled; daily ייטופל לפני resume). אין שינוי-תצוגה → אין redeploy-marker. |
| 2026-06-16 | _(this commit)_ | **fix-post-overflagging:** תיקון סיווג-יתר ב-post_analysis (72/72 סומנו delisted/halt = false-positive תזמוני). שורש: הקולקטור רץ פרה-מרקט; `expected_forward_sessions` ספר את הסשן-הנוכחי גם כשטרם נסגר → navail<exp → סיווג-יתר. תיקון: (1) `completed_forward_sessions`/`expected_forward_sessions` סופרים **רק סשנים שנסגרו** (`session_close ≤ now`, now ניתן-להזרקה); (2) `classify_status` טהורה מבחינה **`forward_pending`** (lag-של-סשן-אחרון, ≤grace, prefix-רציף) מ-gap-אמיתי (חור-פנימי / shortfall>grace / 0-data-אחרי-כמה-סשנים→`delisted_or_halted`). grace=1. TDD: 10 טסטים (`tests/test_post_classify.py`) — לוח-אמיתי (mock רק now → תופס בעיות API ב-session_close), חור-פנימי→halt נשמר, פרה-מרקט→pending. אומת: py_compile + **30/30 suite**. **הוכחה-חיה (dry-run, ללא כתיבה) על 72 המפתחות:** 49 delisted→**0**; 58 partial + 14 partial_gap (gap אמיתי). הטאב החי יתוקן-מעצמו בריצת-EOD הבאה. ⚠️ follow-up תצוגתי: KPI ב-dashboard `_health` סופר `pending_forward` בלבד — להוסיף `forward_pending` לדלי-ה-pending. |
| 2026-06-16 | _(this commit)_ | dashboard-fix (תצוגה): KPI "pending" ב-`_health` סופר עכשיו **גם `forward_pending`** (לא רק `pending_forward`) — אחרת שורות forward_pending הופיעו בטבלה אך נעלמו מה-KPI. שינוי חד-שורתי (`isin([pending_forward, forward_pending])`), אפס שינוי לוגיקת-סיווג. TDD: טסט `test_health_pending_kpi_includes_forward_pending` (RED→GREEN: KPI=1→2). py_compile + **31/31 suite**. redeploy-marker→2026-06-16a. סוגר את ה-follow-up התצוגתי שנפתח בתיקון-הסיווג. |
| 2026-06-16 | _(this commit)_ | dashboard 3 שיפורי-תצוגה (אפס שינוי לוגיקה/נתונים): (1) **הסרת `drop_kind` מטבלאות-הדף** ב-`_watchlist` (קבוע בתוך כל דף; נשאר בנתונים+בדף-הבית; `drop_type` נשמר). (2) **selectbox "מיין לפי" + יורד/עולה** מעל הטבלאות (עומק-צניחה/price/rsi/market_cap/rel-volume/scan_date/ticker; ברירת-מחדל עומק עולה=כמו קודם); ממוין **פעם אחת** על ה-view → 3 טבלאות-הנושא נשארות מיושרות. סינון קיים לא נגע. (3) **עקביות-עיצוב:** helper `page_header(title,sub)` + קבוע `VIEW_ONLY` משותפים; דף-הבית עבר לעברית ("📉 ReboundPro — ניטור"); render/system-health/home אוחדו דרך page_header. אומת: py_compile(5) + **32/32 suite** (4 דפים 0-חריגות · completeness בלי drop_kind · sort-selectbox עובד). redeploy-marker→2026-06-16b. (#4 חלוקה-יומית — צעד נפרד, דורש fetch-חי/איסוף, לא נגעתי.) |
| 2026-06-16 | _(this commit)_ | #4 שלב-קוד: שמירת סדרת-forward יומית (D+1..D+N). שכבה טהורה `daily_series(fwd, ref, scan_date, ticker)` ב-`post_analysis_collector` — **משתמשת מחדש ב-fwd שכבר נמשך (אפס fetch נוסף)**: `cum_pct_from_ref`/`daily_change_pct`/`high_pct`/`low_pct`/day_offset/date. `compute_outcome` מקבל מפתח **additive** `"_daily_rows"` (לא ב-HEADER → `to_matrix` מתעלם → post/classify/אגרגטים **ללא שינוי**). טאב חדש `forward_daily` (long, מפתח scan_date/ticker/day_offset) + `FORWARD_DAILY_HEADER` ב-config. דגל `--backfill-daily` כותב **רק** forward_daily דרך upsert אטומי (`_write_matrix`). אומת: py_compile + **39/39 suite** (32 הקודמים ירוקים = רגרסיה: _daily_rows לא שובר את ה-forward שתוקן הבוקר) + 7 חדשים (daily_series נכון/עקבי/חלקי/ריק · compute_outcome additive · idempotent). `--backfill-daily --dry-run` = 81 שורות/80 אירועים, **ללא כתיבה**. **הכתיבה האמיתית — ידנית בנפרד** (עמיחי). normal/auto post לא כותב forward_daily עדיין (חיווט-auto = צעד עתידי). אין שינוי-תצוגה → אין redeploy-marker. |
| 2026-06-16 | _(this commit)_ | #4 חיווט-auto: ריצת-post הרגילה (auto-EOD) כותבת עכשיו **גם** forward_daily (הסדרה תצטבר לבד יום-יום). helper משותף `_build_daily`+`_write_forward_daily`; **post נכתב קודם**, forward_daily אחריו ב-**try/except** מבודד → כשל-forward_daily רק נרשם (`log.error`) ולא מפיל את הריצה (post כבר נשמר). `--backfill-daily` נשמר (one-off). אפס שינוי ב-`daily.yml` (מריץ את הפקודה ללא-דגל → כותב את שניהם). idempotent (keyed upsert אטומי). TDD: `tests/test_post_auto_forward_daily.py` (3) — (א) ריצה-רגילה כותבת post+forward_daily; (ב) כשל-forward_daily לא מפיל את post (בידוד); (ג) backfill כותב רק forward_daily. אומת: py_compile + **42/42 suite** (39 הקודמים ירוקים = רגרסיה). אין שינוי-תצוגה/workflow → אין redeploy-marker. |
| 2026-06-16 | _(this commit)_ | #4 תצוגתי: כרטיס-המניה מציג את **המסלול היומי forward_daily** (D+1..D+N). תצוגה בלבד, אפס שינוי איסוף/נתונים. ב-`render`: `forward_daily` נוסף ל-`load_many` (NUM_FDAILY) + `restrict` לפי (scan_date,ticker) + מועבר ל-`_stock_card`. בכרטיס (אחרי גרף ה-post): קו **מצטבר-מהכניסה** (cum_pct_from_ref לפי day_offset) + בר **שינוי-יומי** (daily_change_pct) + KPIs (מצטבר-נוכחי + "ימים שנאספו X/HORIZON"). אין-נתונים (אירוע pending) → הודעה ידידותית "0/20 נאספו", לא קריסה. עקבי-עיצוב (plotly/RTL כמו שאר הכרטיס). TDD: `test_stock_card_shows_forward_daily_series` (RED→GREEN). py_compile + **43/43 suite** (כל הדפים 0-חריגות; Stock Card הקיים — intraday/post/fund/news — נשמר). redeploy-marker→2026-06-16c. |
| 2026-06-16 | _(this commit)_ | dashboard-fix: עיצוב גרפי המסלול-היומי בכרטיס בסגנון Investing (תצוגה בלבד). בר "שינוי-יומי" → `go.Bar` עם `marker_color` לפי-סימן (ירוק #26a69a חיובי / אדום #ef5350 שלילי / אפור NaN); קו "מצטבר" → `go.Scatter` lines+markers. שניהם: ציר-x **דיסקרטי "D+n"** (`type=category`, לא 1.0/1.5/2.0), **zeroline** ברור, `ticksuffix="%"`, `template=plotly_white`, `bargap=0.55` (ברים לא נמתחים), `showlegend=False`. helper מקומי `_clean(fig,title)`. נוסף `import plotly.graph_objects as go`. אומת: py_compile + **43/43 suite** (כל הדפים 0-חריגות; הטסט הפונקציונלי של הכרטיס עובר עם הגרפים החדשים). שינוי ויזואלי בלבד — אפס שינוי נתונים/לוגיקה. redeploy-marker→2026-06-16d. |
| 2026-06-16 | _(this commit)_ | dashboard-fix: **תבנית-גרפים אחידה `reboundpro`** לכל 15 הגרפים בכל הדפים (תצוגה בלבד, אפס נתונים). רישום `pio.templates["reboundpro"]` (deepcopy של plotly_white + קווי-רשת דקים אפורים #eee + zeroline #888 מודגש + font/margins/colorway) + `px.defaults.template="reboundpro"` → כל px.* יורש אוטומטית (גם dashboard.py). wrapper `plot(target,fig)` שמרנדר `width=stretch` + **modebar כבוי** (`config displayModeBar=False`) — כל 15 הקריאות עברו דרכו (נקודת-רינדור יחידה). helpers `sign_colors` (ירוק≥0/אדום<0/אפור) + `style_fig` (category/pct). **הגרף הכחול "max-recovery תת-חלון" הוסר** (4 הערכים נשארים בטבלת ה-post). פוצל _clean המקומי → style_fig. TDD: `tests/test_chart_theme.py` (template נרשם+default · sign_colors טהור · כחול-הוסר-במקור). py_compile(5) + **46/46 suite** (43 קודמים = רגרסיה רוחבית: 4 דפים 0-חריגות). הצבעים/grid/modebar = ויזואלי, אימות-עין ב-Cloud. redeploy-marker→2026-06-16e. |
| 2026-06-16 | _(this commit)_ | dashboard-fix: 3 שיפורי-תצוגה לגרפי המסלול-היומי בכרטיס (תצוגה בלבד). (1) **תאריכים אמיתיים** בציר-x ("DD.MM" מעמודת date, `_short`) במקום D+n. (2) **תוויות-ערך** `+/-X.X%` על בר-השינוי-היומי (textposition outside) ועל הקו-המצטבר (top center). (3) **יום-כניסה D0 בצהוב #fbc02d**: בקו — נקודת-D0 שמאלית `cum=0%` מוגדלת+צהובה + annotation "כניסה"; בבר — D0 כקטגוריה שמאלית `y=None` (**בלי בר** — לא ממציאים שינוי) + annotation "כניסה" צהוב. שאר הימים: כחול(קו)/ירוק-אדום(בר). דרך `style_fig`+`plot` הקיימים (template/modebar/category/pct). אומת: py_compile + **46/46 suite** (כל הדפים 0-חריגות; הכרטיס הקיים נשמר). ויזואלי → אימות-עין ב-Cloud. redeploy-marker→2026-06-16f. |

| 2026-06-19 | _(uncommitted)_ | TASK-C parity + יישור HYP-002↔§5. **(A)** יישרתי 3 מקומות-דוק (HYP-002 שורות 51/52/56 + הערת-HYPOTHESES): verdict=Refine = **proxy על DropsLab, לא משחרר M5** (§5 לא נגעתי). **(B)** `research/phase0/taskC_parity.py` (read-only, scope readonly, 0 קריאות-כתיבה) השווה DropsLab `drops_raw` n=4669 מול ReboundPro `watchlist_live` n=**200**. ממצא: קבועי-z + סף −1.7733 **לא מועברים** — פער הגדרה (~28% מצניחות close-to-close מחמיצות את כלל ה-intraday) + יקום (רק 12.7% שורדים רצפת-RB; ה-edge יושב ב-nano/micro ש-RB מוציא). היסט-מצוקה −59→−37 ⇒ הסף יורה 1.7%/30.4% במקום ~10%. תיקון-עובדה: RB=200 (לא ~77), אך ~שבוע בלבד (6/12–18). מזכר: `research/phase0/TASK-C_parity_memo.md`. follow-up TASK-C.1 (Phase-0 על DropsLab∩floor n=591) — דורש אישור נפרד. **אפס נגיעה ב-stubs/M5/scanners/workflows/Sheets-writes.** |

| 2026-06-19 | _(uncommitted)_ | TASK-C.1 — harness edge נטו בתת-הנזיל (`research/phase0/taskC1_liquid_edge.py`, read-only scope readonly, 0 קריאות-כתיבה). DropsLab∩floor n=591 (541 עם D5, 91.5% כיסוי) ⋈ drops_post. **ממצא: אין edge נטו יציב** — long→D5 net **+0.27%**, day-clustered CI95 [−2.94,+2.22] **כולל 0**; לא מנצח SPY נטו (alpha −0.12%). VIX: כיוון משוחזר (VIX<18 net −4.50% שלילי-מובהק; מוגבר חיובי) → תומך-מנגנון כפריור, אך VIX≥20 לא-מובהק (CI [−10.99,+5.24] שביר); רק 18–20 צר מובהק (data-mining). תואם ספרות (edge בלא-נזיל ש-RB מוציא). תיקון-אגב: yfinance `end` עם רכיב-שעה→strftime. מזכר: `research/phase0/TASK-C1_liquid_edge_memo.md`. **proxy/prior — לא M4=GO, לא M5; §5 ללא שינוי.** אפס נגיעה ב-stubs/scanners/workflows/Sheets-writes. |

| 2026-06-22 | `37b8ab2`→(this) | **Vardan-gap פיצ'רים תיאוריים (v0.2.0)** — מיפוי-פער Vardan→ReboundPro (`docs/VARDAN_GAP_ANALYSIS.md`) ובנייה ב-TDD step-by-step עם עצירות-ביקורת: `scanner.atr_14` (Wilder ATR$, מקור-יחיד) + `drop_in_atr` ב-watchlist (intraday open−low / gradual window-drop); **reclaim/drop grid** ב-post_analysis (13 עמ' יום-או-ריק: up%/down% = superset של touched_up/down + `reclaim_atr_day_*x` מ-trough+1=תזמון-אישור, ≠ max_recovery_from_trough=עוצמה); `config.RECLAIM_GRID_COLUMNS` מקור-שמות יחיד; `scanner --backfill-atr` חד-פעמי; חשיפת-dashboard view-only. אומת בקוד: הקולקטור היומי מעבד-מחדש כל אירוע → גריד מתמלא לבד. + pyproject dev-deps + `pythonpath`. **אפס ניקוד/אות/החלטת-כניסה — גבול M5 נשמר.** 98 טסטים ירוקים. |

| 2026-06-23 | `d28c6cc..d91a614` | Live-pages polish (תצוגה בלבד): single-source live table (Investing-style) + הפרדת live/daily נקייה; panel stock-status בקבוצות source-tagged (daily-closes מול live); peak/trough כוללים entry-anchor + live price; daily-move coloring; הוסרו כרטיס-trend מעורפל + שורות DEBUG זמניות. אפס שינוי איסוף/נתונים. |
| 2026-06-24 | `52f0409, 9189b27, 3a487f2` | Entry-Profile descriptive pages (6 intraday / 7 gradual) → Finviz-style metric-separation tables (M5-safe, תצוגה בלבד) + 3 פיצ'רי-כניסה תיאוריים נאספים: `atr_pct`, `dist_sma50`, `dist_sma200`. |
| 2026-06-25 | `3bed4a1, 0b4a835, 3639802, bf17134` | Entry-Profile fixed-horizon split + multi-horizon strip; **Group C pre-registration נעול** (sha256, `0b4a835`) — 6 fundamentals two-sided (quality↔reversal); wiring 6 הפונדמנטלים לטבלת-ההפרדה (display/data-only, M5-safe); תיקון SPY-excess toggle (loud fallback + honest label + strip scope tag). |
| 2026-06-26 | `cef6e05` | **C0 Horizon-Sufficiency pre-registration נעול** (sha256 `aa235fe5`, "האם D+20 מספיק?") — B1/B2 co-primary, אסימטריית-survivorship, B2-פוסק; M5 נשמר. **gate-2 read-only diagnosis:** live 527 אירועים (150 intraday + 377 gradual), frontier ~D+5–9, cap=20 → לא יכול לענות; proxy היסטורי `measured_old` 786,872 ev (mid+high ≈272k) אך **מדוד-רק-D+20** → step-3 **מדד-מחדש** על ה-cache ל-D+90 (subset-שורד יחיד n=241,962 mid+high, 8 משטרים, contamination 1.25% מוחרג): **VERDICT SUFFICIENT — by-default** (B2 median cum_pct שטוח ~0, Δ ל-D+30/60/90 כולם CI∋0 ולא חוצים ε=0.5pp; proxy גבול-עליון, **לא עדות-להיפוך**; תואם TASK-C.1). B1 (δ atr_pct) **כן ממשיך לטפס מעבר D+20** (→ −0.165 ב-D+90, negligible→small) = feature-quality מוחנה (M5-adjacent), **לא שינוי-חלון**. `POST_ANALYSIS_HORIZON` ללא-שינוי; M5 נשמר. גרדיאנט-survivorship לא-מדיד מ-cache-שורד (דורש drops_raw n=4669, מחוץ-לסקופ). branch מוזג ל-main (--no-ff) ונדחף. |

| 2026-06-26 | _(read-only audit)_ | בדיקת-תקינות צינור read-only: חי בריא (watchlist last 6/26, Actions ירוק, 0 כשלים/30 ריצות). **אנומליה יחידה:** כיסוי-fundamentals של gradual **12%** (44/377) מול intraday 79% — Finviz throttle בריצת-ה-EOD ה-bursty (`'NoneType'...find_all'`, נבלע per-ticker). |
| 2026-06-26 | `944f925` | **B1 — retry/backoff ל-fundamentals (התברר ככושל).** retry×4 + pacing + PIT cross-day guard (`select_target_pairs` single-date + `is_pit_refused` past-date) + ביטול stub-FETCH_ERROR. TDD 7 טסטים, suite 186. **תוצאה ב-EOD 6/26: retry-amplification (244 בקשות) החמיר את ה-burst-throttle → כיסוי 50%→0%, runtime→28min (סף-timeout!).** |
| 2026-06-27 | `35b7856` | **GATE-A — de-burst (תיקון-הכיוון) + GATE-C (תיעוד).** אבחנה: Finviz חונק **burst**, לא IP (intraday low-burst = 79% מאותו datacenter-IP). `FINVIZ_FETCH_RETRIES 4→1`, `FINVIZ_FETCH_SLEEP 1.0→6.0`+jitter (~7–8min/ריצה), תקציב **per-run**; PIT-guard + no-stub נשמרו. PROJECT_KNOWLEDGE תוקן (B1 נכשל/החמיר). TDD: אנטי-burst + per-run budget; **suite 187**. **אימות ממתין ל-EOD יום-ב' 29/6** (6s עדיין נכשל → הכרעת-Sharadar). |

## Next
<!-- הוסף כאן ערכים חדשים מתוארכים בכל סוף-סשן -->
